# Calculator Test Cases

Module code: `CALC`

Test case naming: `TC-CALC-[NUMBER]`.

Scope: các phép cộng, trừ, nhân, chia, nối chuỗi, validation dữ liệu, chế độ số nguyên và các control chính của Basic Calculator.

Số lượng: 35 test case (`TC-CALC-001` đến `TC-CALC-035`).

Execution status: 29 Pass, 6 Fail. Các lỗi được liên kết trong test case với Bug `BUG-001` đến `BUG-006`.

Các module đồng cấp khác trong `tests/test-cases` là `build-management`, `ui-controls` và `error-handling`.

Mỗi file test case tuân thủ template QA của dự án gồm Requirement ID, Module / Test type / Technique, Preconditions, Test data, Test steps, Expected result và Status / Related bugs.

