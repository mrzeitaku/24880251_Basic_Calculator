# UI Controls Test Cases

Module code: `UI`

Test case naming: `TC-UI-[NUMBER]`.

Scope: Calculate, Clear, Answer, operation selection, and Integers only controls.

Số lượng: 10 test case (`TC-UI-001` đến `TC-UI-010`).

Execution status: 9 Pass, 1 Fail. `TC-UI-006` liên kết Bug `BUG-006` do Clear không xóa các ô nhập.

